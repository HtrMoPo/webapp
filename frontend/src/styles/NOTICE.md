`main.css` is copied from [htr-united.github.io](https://github.com/HTR-United/htr-united.github.io)
(`src/styles/main.css`) as a design-system starting point/reference (colors,
typography, `.card`/`.chip`/`.form-section`/`.btn` component classes), then
extended here for this app's own views.
